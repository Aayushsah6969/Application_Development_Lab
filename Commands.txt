create venv: python -m venv venv

activate venv: venv\Scripts\activate

add to kernel list: python -m ipykernel install --user --name=venv --display-name "Python (venv)"

jupyter to html:`
pip install jupyter
jupyter nbconvert --to html YourNotebookName.ipynb
